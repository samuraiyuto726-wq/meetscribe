# Async HTTP – leaderboard + activity polling
aiohttp>=3.9.0

# Polymarket official CLOB client (live trading only)
# Handles wallet auth, order signing, and submission
py-clob-client>=0.18.0

# Load .env file automatically
python-dotenv>=1.0.0
