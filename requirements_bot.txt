# Core async HTTP client (used for leaderboard and real order placement)
aiohttp>=3.9.0

# WebSocket client (used for real trade feed; not needed in mock mode)
websockets>=12.0

# Optional: load .env file automatically
python-dotenv>=1.0.0
